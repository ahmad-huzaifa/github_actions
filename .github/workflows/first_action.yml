name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: Print greeting
      run: echo "Hello from GiHub Actions"
    - name: Print goodbye
      run: echo "Step-1 completed bye"
    
    
  
